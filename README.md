#Web Project S7

## To install and run the client
`$ cd client`

`$ npm install`

`$ npm run serve`

## To install and run the server: 
`$ cd server`

`$ npm install server`

`$ npm run start` or `$ nest start`
